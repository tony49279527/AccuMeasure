# Copy to .env.local for local GEO runs. Never commit real secrets.
OPENAI_API_KEY=

# Optional: override the target domain used for citation detection.
GEO_TARGET_URL=https://www.accumeasuretech.com

# Optional: low-cost search model if gpt-4o-mini-search-preview is unavailable.
# GEO_OPENAI_MODEL=gpt-4o-mini-search-preview
